# LLM（DeepSeek 经 LangChain，OpenAI 兼容）
langchain-openai>=0.2
langchain-core>=0.3
# 配置 / 数据
python-dotenv>=1.0
pydantic>=2.0
tiktoken>=0.7          # 短期记忆压缩的 token 估算（纯 CPU，无模型）
# iLink 渠道
requests>=2.31
qrcode>=7.4
# 自助开通 API
fastapi>=0.110
uvicorn>=0.29
# 实时旁观面板（Pusher pub/sub）
pusher>=3.3
# Agent-to-Agent 协议 SDK（Google 官方，含 FastAPI 集成）
a2a-sdk[http-server]>=1.1
# Web 搜索（Tavily：LangChain 官方集成，专为 LLM 优化）
langchain-tavily>=0.2
